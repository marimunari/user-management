// system
import React from "react";

// styles
import { SpinnerContainer, Spinner } from "./Loading.styles";

function Loading() {
  return (
    <SpinnerContainer>
      <Spinner />
    </SpinnerContainer>
  );
}

export default Loading;
